"""Command implementations called by the vibe-radar CLI.

Each module here owns one verb. The thin wrappers under `scripts/` exist for
direct `python -m scripts.X` invocation; they re-export `main` from these.
"""
